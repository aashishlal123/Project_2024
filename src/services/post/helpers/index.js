const { getPostDetail } = require('./general.helpers');
const { getUserPosts, getFollowedPosts } = require('./general.helpers');

module.exports = {
  getPostDetail,
  getUserPosts,
  getFollowedPosts,
};
